# PLAN2 Phase 0 — diagnosis before construction

Branch `plan2/phase0`. Mandate: PLAN2 §2, all four diagnostics, before any sibling
builds. Environment questions were already settled in BRIEF2 §5 and are not
re-derived here.

Every row below is labelled **LEGAL** (a plain `benchmark.runner` evaluation of a
submission trained from random init, no oracle of any kind) or **DIAGNOSTIC** (a
lab instrument that would not be admissible as a submission). No file under
`data/generated/` was read, printed or summarized; every dataset used here was
produced by the public generator with the exact commands recorded in
`lab/gen_p2_grid.sh` and `lab/gen_p2_isolate.sh`.

*(Numbers filled in below; see §9 for the exact commands.)*
